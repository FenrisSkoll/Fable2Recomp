# G1-G1.6 native-renderer research completion

## Status

**RESEARCH MILESTONE COMPLETE — INTEGRATE DOCUMENTATION AND EVIDENCE**

**G2A RETIRED — DO NOT RESUME**

This handoff closes the accepted G1 through G1.6 static-research lineage and
records the local integration boundary. It summarizes the corpus rather than
replacing its phase reports. No historical completion record is rewritten.

## Accepted lineage

Every row is an ancestor of the completed cleanup tip and of the integration
branch. Trees are included so the accepted phase states do not depend on local
branch names.

| Phase | Accepted commit | Tree | Result |
|---|---|---|---|
| G1 | `c44e8c16f4422f9a828caf30899ac989170b8a8c` | `f5bde8e945f2d2ab6764c4d9c38f0f3550cac40c` | `PASS WITH LIMITATIONS`; a pre-packet static Xbox graphics boundary exists |
| G1.5A | `a0d663fc600f74df8e7e87dcf7f18776e23339c9` | `a622e93e586be117a9be0ba9b6554ef3a099636c` | pinned ReXGlue GPU architecture documented |
| G1.5B | `790cf26bc089d94b2102e3df11386bbf2e5ed692` | `35136b97b036ecf981b7e23ebd946c50f648b504` | pinned Xenia Canary architecture documented independently |
| G1.5C | `3cef954120e062974f3fcdf707ac302aa0e2d44e` | `e5bcb4029052927c01f0895ac945fb3baa722496` | 37 source/history divergences classified without assigning Fable causality |
| G1.5D | `78c5d66807abf19f67966bd0c3d8301c29990ae4` | `e1881f9de89824feb051b58300374c924747605b` | `MORE EVIDENCE REQUIRED`; reachability remained at L1 |
| G1.6A | `9af33118bcfbf00f8b446f05e90d18de193410c1` | `fd91d6931e81108d4a619d3875c28b4d66e05187` | `STATIC SEAM QUALIFIED` for one representative texture-fetch method |
| G1.6B | `cd440652451e558b88ba50402721e4cbe82b9a90` | `6a87058d1ef47473e8fa80e9e2882d6cde6b8a7b` | `STATIC COVERAGE NARROW`; the qualified method is not a systemic title seam |
| G2A retirement | `7e8d9e92fff5ca766f9aa12506b6205868e34f62` | `54fe95495f1f733098b42d269ab4eec3c796cef5` | obsolete local branch deleted; history retained; no implementation inherited |

The integration branch
`fable2-native-renderer-research-integration` was created directly from the
G2A-retirement tip. No new commit was added to any completed phase branch.

## Reference and divergence outcome

G1.5A-D compare exact immutable references:

- ReXGlue commit `956c6a8b5da4c54b9899a2593e9c67c26de30194`, tree
  `b78b06b8ac650467372236a3a262864e069a9382`;
- Xenia Canary commit `3a44f20c7bc66db1da583e8a6f0ab740e31908e9`, tree
  `c343b0a5796590fadc3b78c993bfada51e7e9148`; and
- active D3D12-only `rexgpu-xenos.dll`, 2,770,944 bytes, SHA-256
  `8232051BED6E5CE99CF37B2EF581C824F58875C140A4D3C75DE14E8A5DF4AA99`.

The 37-record G1.5C matrix establishes source and primary-history differences,
not title reachability or defect causality. G1.5D keeps those dimensions
separate and concludes that existing Fable evidence reaches L1 only. No row is
causally confirmed and no renderer implementation is selected.

## Static-seam conclusion

G1.6A recovers `SXDK-001` / `sub_82BA77D0` at
`[0x82BA77D0,0x82BA7894)`, size `0xC4`, as one complete six-dword texture-fetch
binding operation. G1.6B then proves that it has exactly two direct calls, is
bypassed by an independent confirmed texture producer, and is also bypassed by
six recovered common state-to-draw roots.

The final conclusion is **STATIC COVERAGE NARROW**. `SXDK-001` remains a
technically qualified narrow method, but it does not authorize interception,
single-owner contract design, instrumentation, or renderer implementation.

## G2A retirement

The retired checkpoint is
`47c2ea2b7d9e14b09fd942c4b5f1bd11c46e2f51`, tree
`910e80108c2d9e7d8474866506f1c9e23ede601c`. It is not an ancestor of the
cleanup tip or integration lineage and is not a substitute for G1.6B. Its 13
checkpoint-added implementation/test/schema paths and its three modifications
to shared files are absent from the accepted lineage. No active-lineage path
required removal.

The local branch `fable2-native-renderer-g2a-forwarding-proof` remains absent.
The checkpoint hash, failed-link evidence, exact path inventory and boundary
limitations remain in the [retirement record](g2a-retirement.md) as historical
evidence. No checkpoint source is restored, copied, merged or archived.

## Integration scope

The audited pre-merge local `main` is
`efd625d0b9635119df85d61d005d754714c2205e`, tree
`2363af6698bbc2e1ade01bfe19e49ca37814e73a`. It is the merge base and an
ancestor of the cleanup tip; the pre-integration left/right count is `0 15`.
There is no local-`main`-only work to resolve or discard.

Those 15 preserved commits include three accepted Phase 4 indirect-target
prerequisite commits before G1. Relative to pre-merge `main`, that inherited
Phase 4 history includes the previously audited `CMakeLists.txt` ReXGlue pin
update from `0.10.0.42-dev.g16d7915` to
`0.10.0.43-dev.g956c6a8`. This integration adds no new build edit and does not
rewrite or squash that prerequisite history.

The G1-G1.6 and G2A-retirement portion being integrated consists of
documentation, deterministic byte-free evidence, versioned schemas and
read-only verifiers. The integration-branch additions are the verifier
correction/tests and this handoff/index entry. A non-fast-forward local merge
preserves every individual phase commit and creates one visible integration
boundary.

## Verifier correction

Before integration, the GPU-reference verifier selected the G1.6A hygiene
range through a two-branch allowlist and required the exact historical G1.6B
branch name. Valid descendants therefore failed despite containing unchanged
accepted evidence.

Commit `59999fff2e32a4e7a79fc5be405a6932827a2398` changes the invariant to:

- accepted G1.6A commit/tree
  `9af33118bcfbf00f8b446f05e90d18de193410c1` /
  `fd91d6931e81108d4a619d3875c28b4d66e05187` must be available and ancestral;
- accepted G1.6B commit/tree
  `cd440652451e558b88ba50402721e4cbe82b9a90` /
  `6a87058d1ef47473e8fa80e9e2882d6cde6b8a7b` must be available and ancestral;
- phase hygiene is computed over the immutable accepted start-to-tip ranges;
- current branch or detached state is informational and appears only in a
  precise failed-lineage diagnostic;
- the retired checkpoint is rejected if it is locally available as an
  ancestor, but its unreferenced object is not required; and
- all existing schema, pin, hash, source mapping, count, link, provenance,
  artifact and phase-path checks remain active.

Focused tests use a temporary bare repository and never switch the real
worktree. They accept the exact G1.6B state, integration and `main`-like
descendants, and a detached descendant. They reject a pre-lineage ref,
tampered evidence/tree identity, the retired checkpoint as a substitute, and
the historical branch name when it points outside the lineage.

### Committed-provenance checkout correction

The first local post-merge check exposed a separate Windows checkout issue.
The historical G1.6 analysis-input record cites the 4,768-byte, SHA-256
`10F9411631AE08D653FB1CDCA192E364CF499BB56948A6E72F93384632786CC7`
analysis-time form of `tools/fable2-entrypoint-closure-evidence.json`. The
immutable accepted G1.6A and G1.6B trees both actually contain Git blob
`e08bca60d9de302fd08c1c6b258b9e29371ab7b7`: 4,658 bytes with SHA-256
`9CF914280FACC388E94D01CFD971F6F75FA2BF10AD509F18F59E3DC220E22F05`.
The prior verifier hashed stale raw worktree bytes and could therefore pass
before a checkout but fail after Git materialized CRLF.

Commit `20ed2ad8e00b9c57738d4328759acb101507b32c` preserves the historical input
record and instead validates the accepted G1.6A blob, accepted G1.6B blob and
current `HEAD` blob through Git. It compares the worktree through Git's clean
filter, so platform line endings are tolerated while substantive worktree or
committed-content changes remain rejected. A focused regression test covers
this invariant. No prior completion report or evidence record is rewritten.

The initial local merge is retained in history. The correction is integrated
through a follow-up local merge rather than resetting, amending or rewriting
`main`; both merge identities are reported in the session handoff.

## Known historical artifact limitation

Aggregate artifact verification may continue to report only these
user-confirmed deleted historical logs:

- `fable2-run-047.1.log`
- `fable2-run-047.log`
- `fable2-run-048.log`

They are not recreated, recovered, substituted, staged or committed. Focused
G1.6B artifact verification is independent of those absences.

## Behavior and handoff boundary

The G1-G1.6 research milestone and its integration-branch commits introduce no
renderer implementation, GPU DLL replacement, runtime behavior, generated
source, manifest, SDK source, new build/package behavior, executable, asset,
save or gameplay behavior. ReXGlue and Canary remain read-only at their pins.
Fable II is not built or executed.

The final local merge commit, tree and both parents are reported in the
session handoff. A commit cannot embed its own resulting identity.
