# G1.5B completion

## Result

**PASS WITH LIMITATIONS**

The exact G1.5A-pinned Xenia Canary GPU source has been audited independently
at symbol level, reconstructed end to end, indexed in deterministic evidence,
and documented without comparing it with ReXGlue or inferring Fable II use.

Limitations are explicit: the work is static-only; the verified clone is
shallow at `049a55f03679b204379b17996bd032ce54bff156`; no concrete adapter,
backend, capability, configuration, workload, driver behavior, or exact Xenos
parity was measured. Source TODOs and synthetic/approximate paths remain
implementation findings, not silently accepted correctness.

## Exact pinned identity and verification

| Field | Value |
|---|---|
| Repository | `https://github.com/xenia-canary/xenia-canary.git` |
| Local clone | `C:\Dev\Fable2NativeRendererResearch\xenia-canary` |
| Branch / upstream | `canary_experimental` / `origin/canary_experimental` |
| Ahead / behind | `0` / `0` |
| Commit | `3a44f20c7bc66db1da583e8a6f0ab740e31908e9` |
| Tree | `c343b0a5796590fadc3b78c993bfada51e7e9148` |
| Parent | `22708301ba76d10aae6f7d7caac8b1cac9e4a8e6` |
| Author date | `2026-08-28T20:19:01-07:00` |
| Committer date | `2026-08-31T22:07:04+02:00` |
| Subject | `[GPU] Replace AC6 ground hack with scalar approximation rounding` |
| State | clean; HEAD exactly equals pin |
| Licence | BSD-3-Clause |
| `LICENSE` SHA-256 | `3D58F25C15634B6EC01D1F133EF798209AE06626AB8D2227B6223D5A9F5113F4` |
| History | shallow; 200 available commits |

No pin was advanced. No fetch or reset was performed.

## Documents and evidence written

- `docs/fable2-gpu-reference/02-xenia-canary-overview.md`
- eight chapters under `docs/fable2-gpu-reference/xenia-canary/`
- `docs/fable2-gpu-reference/evidence/canary-source-inventory.json`
- `docs/fable2-gpu-reference/evidence/canary-subsystem-map.json`
- two versioned Canary schemas under `tools/schemas/`
- extended read-only `tools/Verify-Fable2GpuReference.py`
- README and pin-verification updates
- this completion handoff

The evidence contains repository identities, source-scope Git objects, key-file
blob objects and sizes, symbol locators, relationship call chains,
configuration branches, unresolved questions, and primary-history identities.
It contains no source bodies.

## End-to-end Canary path

```text
xboxkrnl_video.cc:VdSwap_entry / guest ring write pointer
  -> GraphicsSystem::InitializeRingBuffer / UpdateRingBufferWritePointer
  -> CommandProcessor::WorkerThreadMain
  -> COMMAND_PROCESSOR::ExecutePacket
  -> type 0/1 register writes or type 3 handler
  -> COMMAND_PROCESSOR::ExecutePacketType3Draw
  -> D3D12CommandProcessor::IssueDraw
     or VulkanCommandProcessor::IssueDraw
  -> PrimitiveProcessor::Process
  -> RenderTargetCache::Update or IssueCopy -> GetResolveInfo -> Resolve
  -> Shader::AnalyzeUcode
  -> ShaderTranslator::TranslateAnalyzedShader
     -> DxbcShaderTranslator or SpirvShaderTranslator hooks
  -> TextureCache / SamplerInfo / SharedMemory
  -> PipelineCache::ConfigurePipeline
     or VulkanPipelineCache::ConfigurePipeline
  -> descriptors, barriers, host draw, and queue submission
  -> COMMAND_PROCESSOR::ExecutePacketType3_XE_SWAP
  -> backend IssueSwap and TextureCache::RequestSwapTexture
  -> Presenter::RefreshGuestOutput
  -> Presenter::PaintAndPresent
  -> D3D12Presenter::PaintAndPresentImpl
     or VulkanPresenter::PaintAndPresentImpl
```

This is **CONFIRMED source connectivity** at the pinned commit. It is not a
runtime trace.

## Subsystem coverage

Fully documented at static-source level:

- GPU initialization, ownership, memory interface, interrupts, and shutdown;
- ring buffers, indirect buffers, PM4 decoding, waits, events, interrupts, and
  swap packets;
- register metadata/file, writes, binding invalidation, coherency, and triggers;
- draw initiation, primitives, indices, extents, predication, and queries;
- vertex fetch, formats, endian, tessellation/topology conversion;
- texture fetch constants, tiling, conversion, samplers, cache, residency,
  aliasing, and invalidation;
- render targets, depth/stencil, EDRAM, MSAA, ownership, resolves, copies,
  clears, conversion, and scaled readback;
- shader loading, ucode analysis, translation, constants, fetches, memory
  export, DXBC/SPIR-V generation, hashes, and caches;
- pipeline/state assembly, descriptors, root signatures/layouts, specialization,
  persistent caches, and async compilation;
- shared memory, watches, uploads/downloads, lifetime, barriers/layouts, queues,
  fences, and submission synchronization;
- common/backend boundary and D3D12/Vulkan split;
- swap, presentation, pacing, resize, architecturally relevant capture, and
  shutdown; and
- diagnostics, assertions, unsupported behavior, fallbacks, configuration,
  device loss, and TODOs.

Partially determined by static source:

- exact hardware parity of approximation, precision, query, EDRAM, format,
  resolve, and synchronization edge cases;
- device/driver-selected capability and fallback paths;
- runtime cache effectiveness, residency pressure, queue stalls, pacing, and
  loss behavior.

Not documented: none of the thirteen required subsystem areas.

Fable II usage and cross-project divergence are **NOT APPLICABLE**.

## Material branches and static unknowns

Material branches include backend selection; D3D12 RTV/ROV and Vulkan FBO/FSI
render-target paths; occlusion-query modes; async pipeline compilation;
bindless D3D12; sparse shared memory; resolve/memexport readback; texture
budgets and compatibility allowances; resolution scaling; and D3D12/Vulkan
presentation modes.

Important static unknowns:

- actual backend, adapter, extensions/features, surface, and effective cvars;
- guest packet/state/shader/resource use by any title;
- exact Xenos parity where source deliberately approximates or contains TODOs;
- driver compilation and synchronization behavior;
- rationale older than the shallow boundary; and
- external PR/issue identity where no primary link exists in the pinned clone.

## Historical rationale

**CONFIRMED** primary rationale was available for:

- `fbd620c22b44638b66a70bba80d6f30d55a10924` — ZPD queries;
- `9781a75a22ba789124d7f34c6bdb4a85c78b2532` — stale texture prevention;
- `d119505289d540f61ae3d4ba6168f1145625277a` — integer scaling and PWL gamma;
- `a635ac64f5ca37c0b789e8b4166b53dc673b213f` — scaled-resolve downsampling;
- `aed81ca93a1f3e8dd043107babd33438379f379d` — memory invalidation;
- `437a7280cf95310d518a2f68087aab61403956ac` — unified EDRAM layout;
- `3eab2b8b39442e32537610c955fbb8db0c2a6561` — tessellated topology conversion;
- `3a44f20c7bc66db1da583e8a6f0ab740e31908e9` — scalar approximation rounding.

Older rationale and unlinked PR/issue context remain **UNKNOWN**. No
unverified secondary explanation was promoted to confirmed.

## Safety and repository state

No Canary, ReXGlue, Fable runtime, GPU plugin, manifest, generated source,
build, packaging, or G2A code changed. The Canary and SDK repositories remained
read-only. No title was run, no gameplay was automated, and no capture was
created.

No reference clone, binary, build output, source dump, shader, XEX material, or
private asset is tracked or staged by this work.

G1.5B started on branch `fable2-native-renderer-g1.5-reference` at
`a0d663fc600f74df8e7e87dcf7f18776e23339c9`, tree
`a622e93e586be117a9be0ba9b6554ef3a099636c`.

Logical local documentation commits on the branch are:

1. `153d37d92a370c73102eaa00fd760cd83edf9266` —
   `docs(gpu): document pinned ReXGlue GPU architecture`.
2. `a0d663fc600f74df8e7e87dcf7f18776e23339c9` —
   `docs(gpu): finalize G1.5A reference handoff`.
3. `docs(gpu): document pinned Xenia Canary architecture` — the commit
   containing this completion file; its full SHA is reported in the final
   session result because a commit cannot embed its own identity.

## Preconditions for G1.5C

1. Begin from the clean G1.5B ending commit on
   `fable2-native-renderer-g1.5-reference`.
2. Re-run the corpus verifier against exact ReXGlue and Canary pins.
3. Preserve Canary
   `3a44f20c7bc66db1da583e8a6f0ab740e31908e9`; do not silently advance it.
4. Treat both G1.5A and G1.5B as independent baselines.
5. Make divergences explicit and evidence-backed; do not equate newer code with
   correctness.
6. Keep Fable relevance separate until it is demonstrated.
7. Keep paused G2A at
   `47c2ea2b7d9e14b09fd942c4b5f1bd11c46e2f51` unmerged and unmodified.

## Fresh-session handoff

Read `AGENTS.md`, [scope and pins](00-scope-and-pins.md), the
[Canary overview](02-xenia-canary-overview.md), both Canary evidence JSON
files, this completion file, and only then the chapters needed for G1.5C.
Preserve every full SHA and the `UNKNOWN` / `NOT APPLICABLE` boundaries.
Do not begin G2A or renderer implementation from this handoff.
