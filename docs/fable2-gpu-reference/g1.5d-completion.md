# G1.5D completion and fresh-session handoff

## Result: **MORE EVIDENCE REQUIRED**

G1.5D completed the requested static synthesis, exact Fable relevance reclassification, boundary/ownership reassessment, system-UI/presentation contract, reference hypothesis, minimum experiment design and two-part G2 gate. Existing evidence stops at L1 and cannot authorize renderer implementation or assign a cause to the black character surfaces.

## Verified identities

### Fable2Recomp corpus

- accepted G1.5C start branch: `fable2-native-renderer-g1.5-reference`;
- accepted G1.5C start commit: `3cef954120e062974f3fcdf707ac302aa0e2d44e`;
- accepted G1.5C start tree: `e5bcb4029052927c01f0895ac945fb3baa722496`;
- G1 branch/commit/tree: `fable2-native-renderer-g1-audit` / `c44e8c16f4422f9a828caf30899ac989170b8a8c` / `f5bde8e945f2d2ab6764c4d9c38f0f3550cac40c`;
- paused G2A branch/commit/tree: `fable2-native-renderer-g2a-forwarding-proof` / `47c2ea2b7d9e14b09fd942c4b5f1bd11c46e2f51` / `910e80108c2d9e7d8474866506f1c9e23ede601c`.

The G1.5 branch began exactly at the accepted G1.5C commit. Paused G2A was inspected with read-only `git show` and was not checked out, merged, resumed, built or run.

### Fable II GOTY TU1

- title/media/version: `0x4D5307F1` / `0x716F0A0D` / `0.0.1.26`;
- base XEX SHA-256: `88C4EF2E18E65409444D1B068EFF921D1F7E180A5AE64EDC64BA6B0872372662`;
- XEXP SHA-256: `046A05693B4DA4437083C784000A850858B3BF992955C7DB30D518FB3E53E41C`;
- contiguous loaded post-patch image SHA-256: `BF7300F7E0DEEE91444ACD50FBE69752F5CFD3CF51358186F1B849DF25A8CB00`;
- executable fingerprint: `5C8B78B84C93028D166E3DF347206E6642BA40BF374AD1AF8D958B2211320357`;
- manifest SHA-256: `E3EB39CA153E396D5DC53E6F943ED8FF7AF1D6B0704EB860836BD7D21A3F87B0`;
- current accepted `fable2.exe`: 105,042,944 bytes, SHA-256 `EEACEAA8DB38E728B79F4F78B0298B7036E13EB4903518C503199697FA64AE6F`.

### ReXGlue, active artifact and Canary

- ReXGlue path/branch/commit/tree: `C:\Dev\rexglue-sdk-v0.10` / `fable2-v0.10-migration` / `956c6a8b5da4c54b9899a2593e9c67c26de30194` / `b78b06b8ac650467372236a3a262864e069a9382`;
- active and packaged `rexgpu-xenos.dll`: 2,770,944 bytes, SHA-256 `8232051BED6E5CE99CF37B2EF581C824F58875C140A4D3C75DE14E8A5DF4AA99`, D3D12 only;
- Canary path/branch/commit/tree: `C:\Dev\Fable2NativeRendererResearch\xenia-canary` / `canary_experimental` / `3a44f20c7bc66db1da583e8a6f0ab740e31908e9` / `c343b0a5796590fadc3b78c993bfada51e7e9148`.

The SDK remained read-only with only its audited pre-existing `thirdparty/libmspack` materialization. Canary remained clean at the exact pin.

## Evidence dimensions

| Dimension | Counts |
|---|---|
| Source confidence | 36 CONFIRMED SOURCE; 1 PROBABLE SOURCE |
| Fable reachability | 8 CONFIRMED EXISTING RUNTIME EXECUTION; 1 CONFIRMED TU1 STATIC REACHABILITY; 5 PROBABLE FOR FABLE II; 20 UNKNOWN FOR FABLE II; 1 NOT OBSERVED IN RUN 047/048; 2 NOT APPLICABLE |
| Causal relevance | 0 CAUSALLY CONFIRMED; 0 SUPPORTED CANDIDATE; 19 PLAUSIBLE BUT UNCORRELATED; 8 UNKNOWN; 10 NOT RELEVANT |

The highest level actually reached is **L1**: the exact plugin/backend initializes, TU1 has a confirmed swap request path, and presented Fable output exists. L2 packet/draw/copy/query identity is absent.

## Black-surface finding

Confirmed:

- Run 047 screenshots with exact committed hashes show black dog and exposed player skin/head surfaces while environment, UI, lighting and some clothing render.
- Fable executes broad draw, shader/pipeline and presentation paths.
- G1.5C records 37 exact pinned source comparisons.

Unproved:

- the affected character draw IDs;
- shader/pipeline identities for those draws;
- fetch constants, textures and sampler state;
- EDRAM pass/resolve and resource lifetime;
- reachability of any exact divergent candidate branch;
- a cause, fix or controlled clearance.

The strongest evidence priorities are texture/fetch validity and numeric/layout semantics, EDRAM/resolve addressing/conversion, resource invalidation, reset-register ordering, shader scalar precision and pipeline readiness. They are plausible uncorrelated classes, not ranked causes.

## Boundary decisions

- Mechanism: `sub_82BA34D8` — **FORWARDING PROOF ONLY**.
- Presentation: title swap, `VdSwap_entry`, mailbox refresh and D3D12 Present — **PRESENT CORRELATION LATER** if needed.
- Diagnosis: `IssueDraw` with minimal shader/binding/texture/RT/lifetime joins — **TARGETED OBSERVATION LATER**.
- Replacement: representative static Xbox D3D/XDK operations — **STATIC METHOD RECOVERY TARGET**.
- `sub_82B6FA48` remains a title presentation-coordinator hypothesis, not a draw seam.
- `sub_82AAC208` remains discovery-only.
- Raw ReXGlue observation points are not replacement seams.

## Ownership stages

- Stage A: ReXGlue/`rexgpu-xenos.dll` solely owns `IGraphicsSystem`, ring, registers, memory callbacks, interrupts, vblank, submission, presenter and shutdown; ReXApp/UIDrawers owns host UI.
- Stage B: ownership stays unchanged during shadow/title and low-level metadata observation. Original title operations forward exactly once. Incremental rendering is not proved.
- Stage C: prefer recovered title/XDK semantics and optional backend-neutral IR, but exactly one owner remains mandatory for every GPU/runtime/presenter service. The corpus does not prove whether `rexgpu-xenos.dll` can be replaced without a full compatible provider or a new validated integration interface.

No composable dual-GPU ABI is present in the pinned source.

## System UI and presentation

Pinned source requires guest output first, then UI-thread UIDrawer composition, then Present. Exact TU1 and Run 047 evidence confirm Fable executed:

- keyboard import `0x832B9AC4`, thunk `sub_82CC2E90`, direct title call at `0x826CDF60`, runtime log line 42673;
- achievement thunk `sub_82CC27F8`, app `0xFB`, message `0x000B0008`, runtime unlock IDs 11 and 60.

No active Fable-specific keyboard/achievement factory override exists. Dog-naming semantics, confirm/cancel focus behavior, toast display, resize and shutdown remain exact bounded unknowns.

## Experiment backlog

The backlog contains 16 dependency-ordered items: 1 STATIC, 1 BUILD/ABI, 10 RUNTIME METADATA, 1 CONTROLLED A/B and 3 USER CHECKPOINT items. All future runtime records are default-off/fail-open and payload-minimized beneath a verified ignored path.

The first decision-unlocking experiment is `EXP-STATIC-XDK-001`: recover one representative TU1 draw/resource/shader/target method with exact ABI/thread/ownership/coverage, or document the exact reproducible static blocker.

No experiment was run.

## G2 decisions

- Part A: **REVISE G2A BEFORE RESUMING**.
- Part B: **STATIC XDK METHOD RECOVERY**, proceeding **INDEPENDENTLY** of Part A.

Retain the preserved-original wrapper technique and exact-once fixtures, but narrow Part A to linked Release mechanism proof. Defer generalized capture infrastructure and Debug packaging work. The present hook remains useful but insufficient for black-surface diagnosis.

## Change and validation statement

G1.5D changed only documentation, deterministic JSON evidence, versioned JSON schemas and the read-only GPU-reference verifier. It did not change runtime source, ReXGlue, GPU DLLs, manifest, generated source, build/package files, assets, saves, screenshots, reference repositories or execution behavior. It did not build, run, capture, instrument or resume G2A.

The exact documentation/verifier commit and final clean branch commit/tree are reported in the final session handoff; a Git commit cannot embed its own resulting hash without changing that hash.

## Fresh-session handoff

Next gate: **STATIC XDK METHOD RECOVERY**.

Start from this completed G1.5D branch and corpus. Re-verify all pins, read [the G2 decision](11-g2a-reentry-decision.md), [the experiment plan](09-evidence-gaps-and-experiment-plan.md), and [the open questions](open-questions.md), then perform only `EXP-STATIC-XDK-001` if the user explicitly authorizes the new static phase.

Do not resume G2A, implement probes, run Fable II, modify ReXGlue, or begin a renderer under this handoff.
