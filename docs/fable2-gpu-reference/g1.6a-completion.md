# G1.6A completion and fresh-session handoff

## Result: **STATIC SEAM QUALIFIED**

`EXP-STATIC-XDK-001` qualified `SXDK-001` / `sub_82BA77D0` as one
representative title-side Xbox D3D method. The result proves a narrow
high-semantic static route; it does not prove systemic coverage or authorize
implementation.

## Verified phase identity

- required start branch/commit/tree:
  `fable2-native-renderer-g1.5-reference` /
  `78c5d66807abf19f67966bd0c3d8301c29990ae4` /
  `e1881f9de89824feb051b58300374c924747605b`;
- working branch: `fable2-native-renderer-g1.6a-static-xdk-recovery`, created
  directly from that commit;
- G1 accepted base: `c44e8c16f4422f9a828caf30899ac989170b8a8c`;
- paused G2A commit/tree:
  `47c2ea2b7d9e14b09fd942c4b5f1bd11c46e2f51` /
  `910e80108c2d9e7d8474866506f1c9e23ede601c`, not incorporated.

Pinned TU1 loaded image SHA-256:
`BF7300F7E0DEEE91444ACD50FBE69752F5CFD3CF51358186F1B849DF25A8CB00`.
Pinned ReXGlue and Canary commits remained
`956c6a8b5da4c54b9899a2593e9c67c26de30194` and
`3a44f20c7bc66db1da583e8a6f0ab740e31908e9`. The active D3D12-only
2,770,944-byte `rexgpu-xenos.dll` remained
`8232051BED6E5CE99CF37B2EF581C824F58875C140A4D3C75DE14E8A5DF4AA99`.

## Strongest recovered method

- ID/name: `SXDK-001` / `sub_82BA77D0`;
- exact boundary: `[0x82BA77D0,0x82BA7894)`, size `0xC4`, 49 PPC
  instructions;
- ABI: `r3` device state, `r4` fetch slot, `r5` borrowed six-dword fetch
  descriptor, no stack arguments, void-like result;
- operation: one `PM4_SET_CONSTANT` FETCH packet carrying a complete texture
  fetch constant;
- state: device `+0x30` cursor read/write, `+0x38` capacity read,
  `+0x31AC` packet-header bits read;
- direct callers: `sub_82BA7B28` at `0x82BA81CC` and `sub_82BA83C0` at
  `0x82BA85D8`;
- direct callee: `sub_821E8EC0` at `0x82BA77FC`;
- ownership: synchronous producer on the caller thread, asynchronous ReXGlue
  consumer; exact callback thread name and external resource owner remain
  unknown;
- coverage: narrow internal copy/scale paths only; ordinary title binding and
  affected-character coverage are unproved.

Any later forwarding must preserve the packet and address normalization,
cursor/capacity/order, borrowed-pointer behavior, resource lifetime and
original execution exactly once. No wrapper was created here.

## Candidate disposition

| Classification | Count | Candidates |
|---|---:|---|
| `QUALIFIED REPRESENTATIVE METHOD` | 1 | `SXDK-001` |
| `STRONG STATIC CANDIDATE` | 2 | `SXDK-002`, `SXDK-003` |
| `DISCOVERY LEAD ONLY` | 1 | `SXDK-004` |
| `REJECTED AS SEMANTIC SEAM` | 2 | `SXDK-005`, `SXDK-006` |
| `RECOVERY BLOCKED` | 0 | none |

The authoritative records are the
[inventory](evidence/static-xdk-method-inventory.json) and its
[v1 schema](../../tools/schemas/fable2-gpu-static-xdk-method-inventory-v1.schema.json).
The evidence and rationale are reviewed in the
[G1.6A report](12-static-xdk-method-recovery.md).

## Material unknowns

- exact public XDK names and source-level types;
- callback registrar and executing thread identity for `SXDK-003`;
- the owner and retirement join for resources referenced by emitted fetch
  descriptors;
- every alternate fetch-state producer and the fraction of normal title
  rendering covered by this route;
- any relation to the black dog or player-skin rendering defect;
- representative shader, draw, render-target, resolve and EDRAM methods.

## Validation and change boundary

G1.6A changes only documentation, concise non-reconstructive JSON evidence, a
versioned JSON schema and the read-only GPU-reference verifier. Final command
results and the resulting commit/tree are reported in the session handoff
because a commit cannot embed its own hash.

Completed validation before staging:

- `python .\tools\Verify-Fable2NativeRendererG1.py` passed all 11 accepted G1
  candidates;
- `python .\tools\Verify-Fable2EntrypointClosure.py` passed schema 3,
  analyser 2.0.0 and the exact loaded-image identity;
- `python .\tools\Verify-Fable2GpuReference.py --sdk-root
  C:\Dev\rexglue-sdk-v0.10 --canary-root
  C:\Dev\Fable2NativeRendererResearch\xenia-canary` passed G1.5A through G1.6A
  with zero warnings;
- the same command with `--verify-artifacts` ran all checks but did not pass
  because the previously cited `fable2-run-047.1.log`,
  `fable2-run-047.log` and `fable2-run-048.log` are absent from their recorded
  repository-root paths. The two Run 047 files exist only under
  `C:\Dev\Fable2Phase4Seed\runtime-run-047\`; Run 048 was not found under
  `C:\Dev`. No evidence path was silently substituted;
- focused hashing confirmed the G1.6A closure, Ghidra, jump-table and active
  plugin sizes and hashes recorded in the inventory. Therefore the G1.6A
  artifact subset passed, while the aggregate historical artifact gate remains
  explicitly failed for the three missing G1.5D paths.

The temporary `out/g1.6a-static-xdk/` analysis directory was verified ignored,
contained exactly one 13,258-byte helper, and was removed before staging.

No runtime, generated source, manifest, GPU plugin, SDK, build, package,
asset, save or executable behavior changed. Fable II was not built or run.
Paused G2A was not checked out, resumed, copied, repaired, merged or
cherry-picked.

## Fresh-session handoff

Next phase: **G1.6B - Static Seam Qualification and Coverage**.

Run only **`EXP-STATIC-XDK-002`** after explicit authorization: enumerate all
fetch-state producers and all `SXDK-001` callers, recover the
`SXDK-002`/`SXDK-003` callback and lifetime contracts, and quantify ordinary
material/render coverage and bypasses. Keep it static. Do not implement or
intercept the seam, resume G2A, run the game, or begin renderer ownership work.
