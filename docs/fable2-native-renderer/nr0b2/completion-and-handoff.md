# Prepared closeout and continuation

Result: **RECORDER PREPARED — USER CAPTURE REQUIRED**.

Implementation, focused synthetic tests, affected Release compilation,
independent parsing, isolated staging, protected-checkpoint copying and
configuration preflight are complete. The [run card](user-run-card.md) is the
only next action requiring the user. No gameplay or capture has been automated.
No Fable process, actual loaded-module identity, runtime draw count or selected
workload exists for this phase yet. Capture loss/completeness is **unobserved**,
not zero or complete. The known black/blank surfaces remain unexplained.

Both topic branches are `fable2-native-renderer-nr0b2-metadata`. SDK implementation
commit and Fable tooling/preparation commit are pinned in
[implementation and validation](implementation-and-validation.md). Final
documentation HEAD/tree identities are reported externally in the final response.
Only the pre-existing manifest whitespace change and SDK libmspack materialization
remain outside these local commits.

## Resume after user operation — do not prepare again

Use the existing session:
`C:\Dev\Fable2Recomp\out\nr0b2\sessions\nr0b2-oakfield-20260910-002`.
The helper writes `process.json` and runs the analyzer once to create
`analysis.json`. Read these outputs, `preparation.json`, `capture\metadata.bin`,
`capture\status.txt` and the exact allocated log named by `process.json`.
Do not call prepare, recreate this session, delete a claim, allocate a replacement
run or rerun analysis into the same exclusive output file.

Preserve the user's scene/load/stopped-indication/normal-exit/visual/pacing
report verbatim in the reviewed closeout. Verify actual PID/start/end/exit,
loaded EXE/runtime/GPU paths and hashes, all configuration stages and the
NR0B-1 differences. Optional Tracy treatment applies only to the inspected
Release binaries. Inspect source/preserved/baseline integrity after exit;
writable save/cache changes are expected separately.

Review ordered references, accepted trigger and cutoff, complete/partial
intervals, outcomes, bytes/high-water/rejections, final status, shader pairs,
used view/attachment combinations, resolves, deferred/native/submit/completion
joins and all open edges. A zero rejection counter does not close a partial
decision or pre-window dependency. Classify structural validity, consumer
coverage, resource/input completeness, gameplay, visual correctness and
disturbance independently. Never infer named objects from hashes.

Consider at most one candidate under [remaining gates](candidate-and-gates.md).
If none qualifies, record the smallest missing observation; do not automatically
expand instrumentation. A single optional fresh capture-off comparison is
authorized only for a concrete disturbance question, explained before user
operation. No comparison is currently needed or prepared.

If the run fails, preserve all evidence and investigate that specific failure.
If helper analysis failed before writing a report, diagnose it and write any
review into a new path. A normal game exit is not permission to invent a missing
terminal, loaded module or completion edge. After review, add concise capture
analysis/candidate/closeout documents, update the current index, and make logical
local commits. Do not repeat completed preparation.

ReXGlue retains all rendering/runtime ownership. No native draw replacement,
shader/resource payload capture, G2A restoration, guest hook/manifest/codegen
change, source-save change, baseline replacement, dependency upgrade, merge,
push or history rewrite occurred. Stop after NR0B-2; a payload or native-renderer
proof needs its own bounded scope.
