# Phase 2F handoff

Start from AGENTS.md, this directory's report/policy, and the exact bound machine outputs. Do not reopen the frozen mapping decision merely because semantic text is now reachable.

CONFIRMED: explicit approved overlay 15,379 pairs; safe baseline 15,296. All 51,657 semantic terminals reconcile in every lane. Relative to S, 115 gain owner routes (114 previously mapping-blocked and one corrected suppression case). None of those 115 gains independent target-semantic proof. B05 separately enables a native-role/callee proof for the already-routed HammerCombat caller. Reservations remain: 41 new unreserved routes, 74 new reserved routes, plus the independently corroborated but internally reserved HammerCombat proof.

All three unsafe historical associations are removed. Navigator and TROLL_FOOTSTEP remain blocked; __vspltb safely reroutes to `0x83060CD8`. Both same-name physics candidates, three held strong mappings and 715 probable proposals remain excluded. Historical Phase 2C's four associations through held mappings remain historical only. No new mapping-review conflict was found.

Smallest next task: independently inspect the three packets in review-guide.md, beginning with `S-FCEF3DCAA6D1D5FB2EF8AB8F`, caller `0x8229B308 -> 0x8229B038`, reserved callee `0x8229B488 -> 0x8229B1B8`. Preserve comparator intervals `[0x8226DB80,0x8226DBD4)` and `[0x8226D7F8,0x8226D84C)` as internal code regions. Seek one target-native semantic obligation not already consumed in mapping approval. A negative result is acceptable. Do not canonically name functions or revise the overlay automatically.

Registration/free-camera, scripts/banks and E3 preservation remain non-callable/non-runnable. No native state/namespace or content dependency chain was newly established. Nine scalar-access contexts and seven pointer runs gain contextual routes, with zero complete objects/vtables; all 748 boundaries retain their prior classifications. Closure 67, coverage 10 and Ghidra 82 are contextual mapping intersections, not proof. No ownership/indirect/renderer/historical-crash addition intersects these mappings.

All six inherited blockers remain verbatim in source-pins, summary and validation. Historical ownership replay still requires `generated/default/fable2_recomp.136.cpp` SHA-256 `6053CC0EAC4636AA03AAA26581162B707C37E1B52BEE4C10F205D07C63EBDF59`; current SHA-256 remains `D25E664A98833BF9433413336AC92A7376102A67049C0FF35F1270E6BDEB44CB`. Do not search for, reconstruct or replace that input. No temporary runtime instrumentation was introduced.

Use README.md for offline verification. Phase 2E rollback/default behavior is unchanged. No runtime, renderer, manifest, generated source, Ghidra, canonical name, script integration, SDK or proprietary asset change belongs to this phase. No network or publication is authorized.
