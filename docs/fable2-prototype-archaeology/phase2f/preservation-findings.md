# Registration, scripts and historical preservation

## SetUseFreeCamera

CONFIRMED first-party evidence remains the Build-23 name at `0x820BA1FC`, caller `0x82484E28`, callsite `0x82484E58`, construction helper `0x82309378`, adapter `0x822CA4F0`, and callback region `[0x82482298,0x824822A4)`. The three-instruction callback stores incoming r3's low byte at `0x83496BE9`. The payload requests four bytes and stores a four-byte callback at offset zero. The adapter loads the captured callback, obtains an argument, invokes CTR and returns zero. This is native construction evidence, not a callable retail command.

All 206 registration shapes, 2,662 constructor calls and 2,647 complete native name/callback candidates are represented in each applicable lane. No call or shape gains a route through the approved additions. No new registration consumer or callable chain is established.

Two non-SDK diagnostic/interface review leads are newly transported: `_LOADLIB` at `0x82BDEFF8 -> 0x82BDEBF8` (reserved B04), and `Virtual filesystem can not be null` at `0x82C02E68 -> 0x82C02CD0` (unreserved B01). The former is a possible library/Lua-interface lead, not proof of a native Lua owner or registration. The latter is diagnostic wording, not a command. Forty-eight SDK/compiler/vector diagnostic contexts are listed separately. None proves a newly callable debug menu, console, cheat or camera interface.

The existing construction-chain pair `0x8219A600 -> 0x8219AA80` remains routable without an addition. The following exact obligations remain:

| Role | Build-23 address | Approved corresponding TU1 link |
|---|---|---|
| Registration caller | `0x82484E28` | absent |
| Payload helper | `0x82309378` | absent |
| Adapter | `0x822CA4F0` | absent |
| Allocation-chain callee | `0x82227B88` | absent |
| Key/consumer-chain callee | `0x823CFC10` | absent |
| Key/consumer-chain callee | `0x82A23F08` | absent |
| Internal callback | `0x82482298` | no independently owned function route |
| Native Lua/command state lifetime | not established | absent |
| Debug namespace/table owner | not established | absent |
| Corresponding TU1 registration consumer | not established | absent |
| Callable reachability | not established | absent |

The chain-role descriptions are bounded contextual roles, not newly adopted function names. No obligation is waived because the spelling sounds like a command. Community free-camera spellings remain non-accepting. Nothing was invoked, executed or integrated.

## Loose scripts, banks and state ownership

All 160 inventory records are retained: 108 parsed loose Lua 5.1 chunks and 52 inventory-only banks/interfaces. All 54 scripts/scripts_r pairs have the frozen structural relationship. Zero newly connected literal relationships, native binding chains or state/startup owners result from O. The comparison uses the already parsed constant hashes and complete native literals, with the Lua parser's terminator-excluded hash convention explicitly distinguished from native terminator-included hashes. No script was executed or reparsed from an unbound corpus.

Path categories such as game, GUI and startup are not state identities. Structural equality is not semantic identity or retail use. There is no validated proprietary bank-entry layout, bank offset/decompression contract, authenticated retail bank dependency inventory or proven native state/namespace lifetime. These are inherited blockers, not evidence that the engine lacks those features.

## E3/demo and preservation

All 200 preservation candidates remain: 18 native-dependent interface groups and 182 unknown-portability content-path records. Their exact first-party presence, required command names, current-runtime inventory intersections and limitations are retained. All 21 E3 build/path records remain confirmed presence only. Zero new native dependencies or command/interface links connect these preservation records through the overlay.

Quest/help/control text becomes transportable in `0x8230D3C0 -> 0x8230D190`; hero/child/family suitability text becomes transportable in `0x82478138 -> 0x82477298`; childhood growth fields become transportable in `0x827AAD20 -> 0x827A9F88`. These are historically useful native contexts, not connections to an E3 startup script or recovered content consumer. No evidence closes a map, GUI, startup, quest, hero/childhood or presentation content chain to runnable E3 recovery.

The smallest useful preservation follow-up is static inspection of one first-party native registration/state-owner obligation, not loading prototype Lua or guessing a bank format. Assets and scripts do not become redistributable through analysis. No demo, free camera, debug menu or historical content is claimed runnable.

Exact source pointers, literal relationships, per-lane role routes and negative outcomes are in `out/prototype-archaeology/phase2f/registration-debug-freecamera.json` and `script-preservation.json`, bound in the report and validation envelope.
