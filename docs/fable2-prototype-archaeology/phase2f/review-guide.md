# Human semantic review

Do not approve bulk naming or runtime changes. The machine queue preserves every newly connected semantic terminal, all reservations, all target observations and all limitations. The selected tranche is capped at eight owner/category groups; SDK/compiler wrappers are summarized separately. Exact rows are in `evidence/high-value-review.json`; the exhaustive queues are in `out/prototype-archaeology/phase2f/review-queues.json`.

The first coherent task is only three static packets:

1. **HammerCombat exclusion guard** — caller `0x8229B308 -> 0x8229B038`, dependency `0x8229B488 -> 0x8229B1B8`, B05. Confirm the native argument/return interpretation while preserving `internal-code-region-dependent`. It was already owner-routed in S; the new contribution is the callee proof, not a new caller mapping or callee name.
2. **Oxygen field context** — `0x82406F98 -> 0x82405868`, B02. Inspect all three bound literal roles (`MaxOxygen`, `OxygenConsumptionRate`, `OxygenRecoveryRate`). Retain `single-independent-support-class`. Their mapping-consumed identities are not independent semantic votes.
3. **World-map/reward field context** — `0x825240E8 -> 0x82522C10`, B02. Inspect `AppearOnWorldMap`, `RewardMoney`, `RewardRenown` and the duplicate terminal/reference aliases. Retain `single-independent-support-class`; do not infer quest ownership or a complete object from labels.

The remaining selected groups cover `SilverKeysNeeded` (`0x82596030 -> 0x82594BD8`), marriage-related field contexts (`0x8279D208 -> 0x8279C7E8`, `0x827A0120 -> 0x8279F700`), `CECTemporalExperience` (`0x827ECE38 -> 0x827EC6D0`) and `CECSpellManager` (`0x82173CD0 -> 0x82173EB0`). Text remains context, not identity. Full queue groups also retain quest/help, hero/child/family, childhood-growth and combat contexts without crowding the first task.

For any row:

1. Verify the row's source path/size/hash and JSON pointer against source-pins and validation.
2. Follow every recorded mapping edge and its exact batch/decision binding. Read every reservation, including alternative-route and callee dependencies.
3. Inspect the native donor and TU1 instruction roles, full string boundaries, terminators and actual consumers. Do not substitute a prefix, interior reference or nearby string.
4. Check whether the alleged target observation was already consumed by Phase 2D canonicalization or behavior/topology gates. Correlated literals are not independent votes.
5. Repeat the relevant leave-one-mapping and leave-one-batch counterfactual. An owner route or proof must disappear when its necessary addition is omitted.
6. Treat a contradiction as a separate review trigger; quarantine it without changing the approved overlay.

The complete project-intersection queue contains all 83 mappings, including zero intersections. Closure/coverage/Ghidra entries supply priority only. There are no new ownership, indirect, renderer or historical-crash intersections for these additions. The two physics candidates remain excluded review-only evidence with unresolved helper/global obligations. There are no new semantic-conflict packets. No more than the first three packets need human attention to establish a useful next checkpoint.
