# Phase 2G handoff

## Preserved state

Phase 2G is analysis-only. The `phase2e-v1` mapping overlay, every reservation, all three suppressions, both physics exclusions, three held strong mappings, 715 probable mappings, canonical names, manifest, generated source, runtime and all frozen Phase 1–2F bytes remain unchanged. The six inherited blockers remain verbatim, including the blocked historical replay mismatch for `generated/default/fable2_recomp.136.cpp` (`6053CC0EAC4636AA03AAA26581162B707C37E1B52BEE4C10F205D07C63EBDF59` required; `D25E664A98833BF9433413336AC92A7376102A67049C0FF35F1270E6BDEB44CB` current).

## Durable result

- A: independent TU1 byte-result consumers support the contextual exclusion/query role; comparator boundary reservation retained.
- B: complete keyed oxygen-field loading behavior corresponds, but no material independent role vote was found; reservation retained.
- C: a separate TU1 field visitor independently corroborates one destination layout and property-materialization role; reservation retained. Two semantic attributions are quarantined: H1 does not consume reward keys, and `SetObjectiveTag` is a false fourth-terminal alias.

## Smallest next task

Prepare one bounded human owner-review packet for Packet C only. Decide whether to adopt the contextual role label and correct the H1/actual-key-consumer edge in a new non-frozen semantic layer. The packet must preserve the approved owner mapping and `single-independent-support-class`, treat the visitor direction as unresolved, and make no canonical name. Do not rewrite Phase 2F.

If that review declines or completes, the next independent native question is a single TU1 downstream consumer of Packet B storage `+0x34/+0x38/+0x3C/+0x40`; do not reopen corpus-wide mapping archaeology.
