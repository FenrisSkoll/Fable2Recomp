# Phase 2H handoff

## Durable result

Packet C alone now has an owner-reviewed, non-canonical analyst role: “conditional keyed reward/world-map field materializer.” The append-only correction distinguishes the two-word handle resolver `0x821B2528 -> 0x821B24F8`, scalar consumer `0x823C0588 -> 0x823BF820`, and Boolean-like consumer `0x82310448 -> 0x82310290`. It rejects `SetObjectiveTag` as a false high-half alias.

The owner mapping `0x825240E8 -> 0x82522C10`, reservation `single-independent-support-class`, and unresolved independent-visitor direction are unchanged. Packet A, Packet B, every other semantic row, and canonical naming remain unapproved. Phase 1–2G stay frozen.

## Analyst-facing use

Use the read-only consumer documented in `README.md`. Default mode returns no Phase 2H reviewed role. Exact `phase2h-v1` opt-in binds four repository-relative paths and hashes, then returns only the bounded Packet C materialized view. Never treat the role as a function name or feed the delta into a mapping consumer.

## Inherited blockers

All six blockers are copied verbatim in `evidence/source-pins.json`, `evidence/reviewed-semantic-summary.json`, and `evidence/validation.json`. In particular, historical ownership replay remains blocked by the known `generated/default/fable2_recomp.136.cpp` mismatch; do not search for, reconstruct, replace, or regenerate it.

## Smallest next task

Plan an explicit opt-in analyst-facing annotation/comment layer that consumes the exact Phase 2H view without writing production names, mappings, manifests, Ghidra state, runtime code, or generated code. Define its display boundary and refusal behavior before implementation. Further corpus-wide mapping archaeology is not justified by this decision.
