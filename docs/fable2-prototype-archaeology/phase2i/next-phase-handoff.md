# Phase 2I practical handoff

Use `Fable2AnalystAnnotations.py query` during ordinary recompilation work when
a TU1 address needs Build-23 correspondence or semantic context. Start with the
closed mapping view or no mapping; opt into Phase 2E/2F/2H only when the exact
bounded evidence is needed. Use the address profile for a durable review packet
and avoid bulk comments.

Packet C remains the sole owner-reviewed contextual role. Packet A/HammerCombat
and Packet B/oxygen remain static review evidence without owner approval or
function names. Suppressed, held, probable and physics pairs remain inactive.
All six inherited blockers remain unchanged.

When an analyst needs to inspect those inactive proposals, use the explicit
`excluded-review` export: it contains exactly 720 warning-only records (two
physics candidates, three held strong mappings and 715 probable mappings) and
does not add a correspondence route.

The generated Ghidra plan is intentionally data-only. The smallest justified
next integration, if desired, is a separately authorized importer trial on a
disposable copy of a Ghidra project. It must verify exact TU1 image identity,
append rather than overwrite, enforce the stale-hash conflict rule, and prove
exact removal before any real analyst database is considered.

Absent that separate authorization, stop archaeology here and use the query and
export layer as a read-only aid during normal Fable II recompilation work.
